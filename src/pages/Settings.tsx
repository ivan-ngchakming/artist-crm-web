import { PageContainer, PageHeader } from "../components";

const Settings = () => {
  return (
    <PageContainer>
      <PageHeader title="Settings" />
    </PageContainer>
  );
};

export default Settings;
